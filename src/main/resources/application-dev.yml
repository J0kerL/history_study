spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/history?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 1234
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: 1234

mybatis:
  configuration:
    # 使用 Slf4jImpl 将 SQL 日志交由 Logback 统一管理（配合 logback-spring.xml 中 mapper 包的 DEBUG 配置）
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

knife4j:
  basic:
    # 开发环境关闭
    enable: false