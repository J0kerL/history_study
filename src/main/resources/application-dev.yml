spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/history?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 1234
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: 1234

mybatis:
  configuration:
    # 开发环境打开标准输出 SQL，便于调试 Mapper 行为。
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

knife4j:
  basic:
    # 开发环境关闭
    enable: false